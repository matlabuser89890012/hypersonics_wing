% Function to calculate temperature at a given point

function temperature = calc_temperature(heat_flux, distance_from_surface)
    % Simplified temperature calculation (for demonstration)
    temperature = heat_flux * distance_from_surface;
end
